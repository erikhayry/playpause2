<!--TODO make fixed-->
{{#if error}}
<div class="alert alert-danger" role="alert">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close" {{action "closeAlert"}}>
        <span aria-hidden="true">&times;</span>
    </button>
    {{error}}
</div>
{{/if}}

{{ember-webview
        onReady='onWebviewReady'
        url=stationUrl
}}
