interface Settings {
    browserWindow: {
        height:number;
        width: number;
    }
}

let settings:Settings = {
    browserWindow: {
        height: 1100,
        width: 1900
    }
};

module.exports = settings;
