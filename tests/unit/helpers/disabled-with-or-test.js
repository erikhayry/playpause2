
import { disabledWithOr } from 'play-pause/helpers/disabled-with-or';
import { module, test } from 'qunit';

module('Unit | Helper | disabled with or');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = disabledWithOr([42]);
  assert.ok(result);
});

